import React from 'react';
import { useParams } from 'react-router';

function Cart(){

    //const {product} = useParams();
    
    return(
        <div>
            <h1>Carrito</h1>
            {/*product.map((p) =>(
                p.titulo
            ))*/}
        </div>
    );
}

export default Cart;